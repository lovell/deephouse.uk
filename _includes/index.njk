---
title: Deep House Mixes - London, UK
image: decks
eleventyComputed:
  description: "A selection of {{ pagination.items.length }} deep house DJ mixes, all recorded live."
layout: layout.njk
---
<article class="card-body pb-3">
  <div class="row row-cols-2 row-cols-sm-3 row-cols-xl-6 g-4">
    {% for mix in pagination.items %}
    <div class="col">
      <div class="card h-100" style="background-color: #{{ mix.colour }}33">
        <picture>
          <source srcset="/images/mix/{{ mix.slug }}.avif" type="image/avif" />
          <img
            src="/images/mix/{{ mix.slug }}.jpg"
            class="card-img-top"
            style="aspect-ratio: 1/1"
            alt="{{ mix.title }} - Cover"
            {% if loop.index > 12 %}loading="lazy"{% endif %}
          />
        </picture>
        <span
          class="position-absolute top-0 end-0 badge rounded-pill m-2"
          style="background-color: #{{ mix.colour }}"
        >
          <span style="color: #{{ mix.colourContrast }}">
            {% if mix.popularity > 42 %}&#9654; {{ mix.popularity }}{% else %}NEW{% endif %}
          </span>
        </span>
        <div class="card-body">
          <h5 class="card-title">{{ mix.title }}</h5>
          <h6 class="card-subtitle">{{ mix.month }}</h6>
          <a
            href="/mix/{{ mix.slug }}/"
            class="stretched-link"
            title="{{ mix.title }}"
          ></a>
        </div>
      </div>
    </div>
    {% endfor %}
  </div>
</article>